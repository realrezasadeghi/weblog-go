package constants

const (
	JwtAccessTokenTimeDuration = 10
	JwtAccessTokenIssuer       = "develop"
	AccessTokenCookie          = "access-token-cookie"
	JwtAccessTokenSecret       = "can-you-keep-a-secret"
)
